import React from 'react';

/**
 * ButtonLoader Component.
 * Embedded micro spinner to show inside buttons during actions.
 * @returns {JSX.Element} Button spinner.
 */
export const ButtonLoader = () => {
  return (
    <div className="button-loader-spinner" aria-hidden="true" />
  );
};
