/**
 * Theme configurations for EAM dashboard styling logic.
 * Defines theme identifiers and associated HTML document body CSS classes.
 */
export const THEME_CONFIG = {
  THEMES: {
    LIGHT: 'light',
    DARK: 'dark',
  },
  CLASSES: {
    LIGHT: 'light-theme',
  },
};
